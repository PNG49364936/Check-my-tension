<!DOCTYPE html>
<html lang="fr">
  <head>
    <title>Check My Tension</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "custom", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%# Header sticky %>
    <header class="site-header">
      <div class="site-header-inner">
        <%= link_to root_path, class: "site-logo" do %>
          <div class="pulse-dot"></div>
          <span class="site-logo-text">Check<span>My</span>Tension</span>
        <% end %>
        <nav class="site-nav">
          <%= link_to "Accueil", root_path %>
        </nav>
      </div>
    </header>

    <%# Flash messages %>
    <% if notice %>
      <div style="max-width:1200px;margin:0 auto;padding:1rem 1.5rem 0;">
        <div class="flash-notice">
          <span>&#10003;</span> <%= notice %>
        </div>
      </div>
    <% end %>
    <% if alert %>
      <div style="max-width:1200px;margin:0 auto;padding:1rem 1.5rem 0;">
        <div class="flash-alert"><%= alert %></div>
      </div>
    <% end %>

    <main>
      <%= yield %>
    </main>

    <footer style="text-align:center;padding:2rem 1.5rem;color:var(--text-light);font-size:0.78rem;border-top:1px solid var(--border);margin-top:2rem;font-family:'Source Serif 4',serif;font-style:italic;">
      Check My Tension &mdash; Suivi personnel de la tension art&eacute;rielle
    </footer>
  </body>
</html>
